#include <bits/stdc++.h>
using namespace std;

void solve() {
    int H, M;
    cin >> H >> M;

    cout << (H - 9) * 60 + M << '\n';
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    solve();
    return 0;
}
